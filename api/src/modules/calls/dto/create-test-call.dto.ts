import { CreateCallDto } from './create-call.dto';

/** A live test call: name + phone, optionally linked to an existing contact for CRM personalization. */
export class CreateTestCallDto extends CreateCallDto {}
